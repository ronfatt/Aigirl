import type { FieldError, UseFormRegisterReturn } from "react-hook-form";
const base = "min-h-12 w-full border-b border-black/25 bg-transparent py-3 text-base outline-none transition placeholder:text-black/40 focus:border-deep-red";
export function Field({ label, error, register, type = "text", placeholder }: { label: string; error?: FieldError; register: UseFormRegisterReturn; type?: string; placeholder?: string }) { return <label className="block text-[10px] font-bold uppercase tracking-[.18em]">{label}<input type={type} placeholder={placeholder} className={base} aria-invalid={!!error} {...register} />{error && <span role="alert" className="mt-1 block text-xs normal-case tracking-normal text-deep-red">{error.message}</span>}</label>; }
export function TextareaField({ label, error, register }: { label: string; error?: FieldError; register: UseFormRegisterReturn }) { return <label className="block text-[10px] font-bold uppercase tracking-[.18em]">{label}<textarea rows={5} className={base} aria-invalid={!!error} {...register} />{error && <span role="alert" className="mt-1 block text-xs normal-case tracking-normal text-deep-red">{error.message}</span>}</label>; }
