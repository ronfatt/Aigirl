"use client";
import { useEffect, useState } from "react";
export function CustomCursor() { const [state, setState] = useState({ x: -50, y: -50, label: "", active: false }); useEffect(() => { if (!matchMedia("(pointer: fine) and (prefers-reduced-motion: no-preference)").matches) return; const move = (e: MouseEvent) => { const target = (e.target as HTMLElement).closest<HTMLElement>("[data-cursor],a,button"); setState({ x: e.clientX, y: e.clientY, label: target?.dataset.cursor ?? "", active: !!target }); }; addEventListener("mousemove", move); return () => removeEventListener("mousemove", move); }, []); return <div aria-hidden="true" className={`pointer-events-none fixed left-0 top-0 z-[120] hidden items-center justify-center rounded-full bg-ivory text-[8px] font-bold uppercase tracking-widest text-black transition-[width,height] duration-200 lg:flex ${state.active ? "h-12 w-12" : "h-2 w-2"}`} style={{ transform: `translate3d(${state.x}px,${state.y}px,0) translate(-50%,-50%)` }}>{state.label}</div>; }
