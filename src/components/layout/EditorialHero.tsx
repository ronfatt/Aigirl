import { Container } from "@/src/components/ui/Container";
export function EditorialHero({ eyebrow, title, intro, dark = false }: { eyebrow?: string; title: string; intro: string; dark?: boolean }) { return <header className={`${dark ? "bg-black text-ivory" : "bg-ivory text-black"} pb-20 pt-36 sm:pb-28 sm:pt-44`}><Container><p className="text-[10px] uppercase tracking-[.23em] opacity-55">{eyebrow ?? "Batik NXT Archive"}</p><h1 className="mt-8 font-display text-[clamp(4.2rem,12vw,12rem)] uppercase leading-[.7] tracking-[-.06em]">{title}</h1><p className="mt-12 max-w-xl text-base leading-7 opacity-60 sm:ml-auto sm:text-lg">{intro}</p></Container></header>; }
