"use client";
import Image from "next/image";
import { useEffect, useRef } from "react";
import { ArrowDown } from "lucide-react";
import gsap from "gsap";
import { content } from "@/src/content/en";
import { Button } from "@/src/components/ui/Button";
export function HeroSection() { const root = useRef<HTMLElement>(null); useEffect(() => { if (matchMedia("(prefers-reduced-motion: reduce)").matches) return; const ctx = gsap.context(() => { gsap.fromTo(".hero-media", { scale: 1.08 }, { scale: 1, duration: 1.5, ease: "power3.out" }); gsap.from(".hero-line", { yPercent: 120, stagger: .12, duration: 1.1, ease: "power4.out", delay: .2 }); gsap.from(".hero-copy", { opacity: 0, y: 15, duration: .7, delay: .75 }); }, root); return () => ctx.revert(); }, []); return <section ref={root} className="relative min-h-[100svh] overflow-hidden bg-black text-ivory"><Image src="/images/hero/home.svg" alt="Batik NXT crimson couture campaign" fill priority sizes="100vw" className="hero-media object-cover object-[58%_35%] sm:object-center"/><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/5 to-black/20"/><div className="absolute inset-x-0 bottom-0 z-10 grid gap-8 px-5 pb-9 sm:px-8 lg:grid-cols-[1fr_22rem] lg:items-end lg:px-12 lg:pb-12"><div><p className="hero-copy mb-5 text-[10px] font-semibold uppercase tracking-[.23em] text-white/75">{content.hero.eyebrow}</p><h1 className="font-display text-[clamp(4rem,10vw,10rem)] uppercase leading-[.72] tracking-[-.055em]">{content.hero.title.map((line) => <span key={line} className="block overflow-hidden"><span className="hero-line block">{line}</span></span>)}</h1></div><div className="hero-copy max-w-sm"><p className="mb-7 text-sm leading-6 text-white/75 sm:text-base">{content.hero.body}</p><Button href="/collections/crimson-heritage">Explore Collection</Button></div></div><div className="absolute right-5 top-1/2 hidden -translate-y-1/2 flex-col items-center gap-3 text-[8px] uppercase tracking-[.25em] text-white/65 sm:flex"><span className="[writing-mode:vertical-rl]">Scroll to discover</span><ArrowDown className="size-3"/></div></section>; }
