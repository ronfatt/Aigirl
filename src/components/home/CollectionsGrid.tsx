import Link from "next/link";
import type { Collection } from "@/src/types";
import { ResponsiveImage } from "@/src/components/media/ResponsiveImage";
export function CollectionsGrid({ collections }: { collections: Collection[] }) { return <section className="bg-black py-24 text-ivory"><div className="px-5 sm:px-8 lg:px-12"><p className="mb-10 text-[10px] uppercase tracking-[.22em]">05 · Collections Archive</p><div className="group flex flex-col gap-3 lg:h-[75svh] lg:flex-row">{collections.map((collection) => <Link key={collection.id} href={`/collections/${collection.slug}`} className="group/panel relative min-h-[60svh] overflow-hidden lg:min-h-0 lg:flex-1 lg:transition-[flex] lg:duration-700 lg:hover:flex-[1.55]" data-cursor="view"><ResponsiveImage asset={collection.coverImage} className="absolute inset-0 h-full" imageClassName="transition duration-700 group-hover/panel:scale-105" sizes="(max-width:1024px) 100vw, 40vw"/><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-black/10"/><div className="absolute inset-x-0 bottom-0 p-6"><p className="text-[9px] uppercase tracking-[.2em] text-white/55">{collection.year} · {collection.number}</p><h3 className="mt-2 font-display text-4xl uppercase leading-none lg:text-5xl">{collection.title}</h3><p className="mt-5 text-[9px] uppercase tracking-[.2em]">Explore →</p></div></Link>)}</div></div></section>; }
