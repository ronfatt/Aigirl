import Link from "next/link";
import { ResponsiveImage } from "@/src/components/media/ResponsiveImage";
import { Container } from "@/src/components/ui/Container";
import { formatDate } from "@/src/lib/utils";
import type { JournalArticle } from "@/src/types";
export function JournalPreview({ articles }: { articles: JournalArticle[] }) { return <section className="bg-warm-white py-24 text-black sm:py-32"><Container><div className="mb-12 flex items-end justify-between"><div><p className="text-[10px] uppercase tracking-[.22em]">06 · Journal</p><h2 className="mt-4 font-display text-6xl uppercase sm:text-8xl">Notes on form</h2></div><Link href="/journal" className="hidden text-[10px] uppercase tracking-[.2em] sm:block">View all →</Link></div><div className="grid gap-10 md:grid-cols-3">{articles.map((article, i) => <Link href={`/journal/${article.slug}`} key={article.id} className={`group ${i === 1 ? "md:mt-20" : ""}`}><ResponsiveImage asset={article.coverImage} className="aspect-[4/3]" imageClassName="transition duration-700 group-hover:scale-[1.025]"/><div className="mt-5 flex gap-3 text-[9px] uppercase tracking-[.16em] text-black/45"><span>{article.category}</span><span>{formatDate(article.publishedAt)}</span><span>{article.readingTime} min</span></div><h3 className="mt-3 border-b border-black/15 pb-4 font-display text-3xl decoration-deep-red underline-offset-8 group-hover:underline">{article.title}</h3></Link>)}</div></Container></section>; }
